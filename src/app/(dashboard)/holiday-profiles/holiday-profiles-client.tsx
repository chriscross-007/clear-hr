// CLE-167 — Superseded by Profileless Holiday Management. This file is no
// longer imported (page.tsx now redirects to /employees). It is kept only
// to avoid a missing-file error if something still references it; will be
// deleted in the Phase 7 cleanup pass.

export function HolidayProfilesClient() {
  return null;
}
